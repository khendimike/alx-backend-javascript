/**
 * A function which display a message to the STDOUT
 * @param {String} message the message to be display
 */
const displayMessage = (message) => {
  console.log(message);
};

module.exports = displayMessage;
